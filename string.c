#include "main.h"
/**
 *_strlen - function that returns length
 *@ptr: to be checked
 *Return: j
 */
int _strlen(char *ptr)
{
int j;
for (j = 0; ptr[j] != 0; i++)
;
return (j);
}
/**
 *_strlent - constant character pointer string
 *@ptr: to be checked
 *Return: j
 */
int _strlent(const char *ptr)
{
int j;
for (j = 0; ptr[j] != 0; i++)
;
return (i);
}
